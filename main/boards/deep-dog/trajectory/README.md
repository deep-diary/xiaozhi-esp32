# 轨迹规划模块 (Trajectory)

## 功能概述

对电机控制进行**轨迹规划**，实现平滑的关节运动。用于：
- 单腿或单关节的点对点平滑移动（如站立/卧倒过程）；
- 跳舞等动作序列的关键帧插值。

当前实现为 C 接口，支持点对点规划、三次样条插值，可与 `leg` / `dog` 层配合使用。

---

## 目录与文件

- `trajectory_planner.h`：接口与数据结构定义
- `trajectory_planner.c`：实现（点对点、插值、样条等）

---

## 在机器狗中的角色

- **上层**：`dog`（整机）或 `leg`（单腿）在需要平滑运动时调用本模块。
- **下层**：本模块只产出轨迹点（位置/速度、时间），不直接发 CAN；由调用方将每个轨迹点转换为电机目标位置，再通过 `motor` 下发。
- **电机数量**：当前头文件中 `TRAJECTORY_MAX_MOTOR_COUNT` 为 6。机器狗单腿 3 个关节，可：
  - 按腿使用：每条腿一个 planner，每个点 3 个电机；或
  - 后续扩展为 12 路（4 腿×3）或保持按腿分拆使用。

---

## 主要接口（简要）

| 接口 | 说明 |
|------|------|
| `trajectory_planner_init` | 初始化规划器 |
| `trajectory_plan_point_to_point` | 两点间规划（带速度/加速度约束） |
| `trajectory_interpolate` | 对已有轨迹点做插值 |
| `trajectory_get_next_point` | 获取下一个轨迹点（回放用） |
| `trajectory_cubic_spline_interpolate` / `trajectory_cubic_spline_stream` | 三次样条插值 |
| `trajectory_is_finished` / `trajectory_reset` | 状态与重置 |

---

## 使用约定

- 单位：位置一般为**弧度**，与 motor/leg 一致；时间单位为 ms。
- 调用方负责：按周期（如 20ms）取点 → 换算到各电机 ID → 调用 motor 层设置目标位置。
- 跳舞：可预先定义关键帧（多组关节角），用本模块做插值得到平滑轨迹，再按时间线执行。

---

## 开发计划（与本板相关）

- [ ] 确定机器狗使用方式：按腿（3 电机）还是整机（12 电机）扩展。
- [ ] 在 leg 或 dog 中接入：站立/卧倒过程使用点对点；跳舞使用关键帧 + 插值。
- [ ] 如需 12 路，可在本模块扩展最大电机数或在外层做 4×3 的拆分。
