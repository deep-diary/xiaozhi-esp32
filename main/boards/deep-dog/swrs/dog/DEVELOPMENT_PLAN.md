# Deep-Dog 运动开发计划

仅覆盖**四足运动域**（CAN / 电机 / 腿 / 整机 / 触摸 / 安全 / IMU / RK / ROS）。

视觉、HTTP、人脸、MediaMTX：**不在本文** → [../ROADMAP.md](../ROADMAP.md)、[../vision/](../vision/)。

**文档角色**：运动阶段「已完成 / 进行中 / 待办」与验收。

---

## 1. 产品定位与目标

| 维度         | 说明                                                                                                                                                                                                           |
| ------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **核心场景** | 语音（小智 / MCP）、触摸与网页遥控控制四足；视觉/人脸/推流见上级 [ROADMAP.md](../ROADMAP.md)。远期 RK3588 / ROS。 |
| **技术路径** | 单路 CAN + 12 个无刷关节电机；关节轨迹在本板规划，不依赖 URDF。                                                                                                                                                |
| **原则**     | 新代码优先落在 `deep-dog/`；必须改主工程时仅做 Kconfig/板型分支级最小补丁。                                                                                                                                    |

---

## 2. 当前能力基线（已实现，便于对照新需求）

以下依据当前代码与 `dog/README.md`、`touch_btn/README.md` 等整理，排期时视为**基线**。

| 能力                                                | 模块                                     | 状态说明                                                                             |
| --------------------------------------------------- | ---------------------------------------- | ------------------------------------------------------------------------------------ |
| CAN / TWAI                                          | `can/`                                   | 板级初始化与收发                                                                     |
| 电机协议、12 电机注册、使能、位置模式初始化         | `motor/`                                 | 协议含运控/位置等模式枚举；整机初始化走**位置模式 + 速度限制**等参数                 |
| 单腿正弦步态、站立/卧倒、机械/行走软限位            | `leg/`                                   | 与 `GaitPlanner` 步序对齐                                                            |
| 整机 `DogControl`、对角 Trot / 四腿同步步态切换     | `dog/`                                   | `goForward` / `goBack`、多小步、**一大步**（半周期内多小步+间隔）、持续前进/后退任务 |
| 姿态状态机（趴下先站立再行走、持续走与单步关系等）  | `dog/dog_state_machine.h`                | 可供灯带/屏显                                                                        |
| 跳舞（站立 → 2× 前进一大步 → 2× 后退一大步 → 站立） | `dog/`                                   | 时序固定，**未**接 `trajectory` 插值                                                 |
| 语音 / MCP：`self.dog.*`、`self.chassis.*` 等       | `esp_sparkbot_board.cc`                  | 调用 `DogControl`，非 UART 底盘                                                      |
| 触摸：init 开窗、小步/大步/持续走、站立/趴下        | `touch_btn/`                             | 与 `DogControl` 绑定                                                                 |
| 轨迹规划库（点对点、样条、插值 API）                | `trajectory/`                            | **库已存在**，**尚未**接入大步态/跳舞主路径                                          |
| 动作库：晃身、横移、跳舞扩展等                      | `dog/` + MCP / 触摸                      | **阶段八已交付**（左右晃身、左右平移步态、编排跳舞等，见阶段八验收）                 |
| HTTP / 人脸 / 推流（视觉轨） | `http-server/`、`face_ai/` | **不在本文展开** → [../ROADMAP.md](../ROADMAP.md) |

**运动域缺口（基线外）**：

- **步态补齐**：左转/右转等若仍有缺口则继续迭代。
- **轨迹优化**：跳舞/大步与 `trajectory` 深度结合仍为可选。
- **阶段六 / 七 / 九**：力矩保护、大步插值、IMU（见下文）。

---

## 3. 路线图总览

| 阶段   | 主题                                                                        | 优先级建议           | 与基线关系                                               |
| ------ | --------------------------------------------------------------------------- | -------------------- | -------------------------------------------------------- |
| 一～五 | 单电机→单腿→整机→语音 MCP→跳舞与触摸                                        | **已完成（主体）**   | 见下文历史阶段表                                         |
| 六     | **安全：堵转与最大力矩保护**                                                | 高（硬件保护）       | 新增                                                     |
| 七     | **运动品质：大步插值 + 运控模式**                                           | 高（体验与性能）     | 在现有步态与 `motor` 上增强                              |
| 八     | **动作库：晃身、横移、丰富跳舞**                                            | —                    | **已完成**                                               |
| 九     | **IMU：BMI270 + MQTT `imu/status`**                                       | 中（感知/闭环前置）  | 对齐 thumble；对外含 [M01](../mqtt/M01-board-mqtt-protocol.md) |
| 十     | **视觉（已迁出）** | — | 见 [../ROADMAP.md](../ROADMAP.md)（V-S01～V-C04） |
| 十一   | **RK3588 CAN 与小智协同** | 中高 | 协议与网关设计 |
| 十二   | **ROS：状态/控制上报 rviz**                                                 | 中（调试与仿真对齐） | 桥接层                                                   |
| 十三   | **ROS：话题/服务下发控制机器狗**                                            | 中                   | 与十二可同一套桥接                                       |

---

## 4. 历史阶段（一～五）— 任务与完成标准

### 阶段一：单电机与 CAN 验证

| 任务                   | 负责模块           | 完成标准           |
| ---------------------- | ------------------ | ------------------ |
| CAN 初始化与引脚       | `can` + `config.h` | TWAI 正常收发      |
| 电机协议与 12 电机注册 | `motor`            | 使能及位置指令可达 |
| 单电机 MCP（可选）     | `motor` / 板级     | 至少一轴可调试动作 |

**验收**：至少 1 个电机可通过 MCP 或代码使能并移动到指定角度。

---

### 阶段二：单腿控制

| 任务                       | 负责模块 | 完成标准            |
| -------------------------- | -------- | ------------------- |
| `LegControl` 与限位/站立位 | `leg`    | 单腿三关节协调      |
| 站立/卧倒/正弦迈一步       | `leg`    | 与 `motor` 联调通过 |

**验收**：单腿可站立、卧倒、向前/向后迈一步。

---

### 阶段三：整机步态

| 任务                    | 负责模块                | 完成标准          |
| ----------------------- | ----------------------- | ----------------- |
| `DogControl` + 四腿协同 | `dog`                   | Trot 等步态可循环 |
| 前进/后退/停            | `dog` + `leg` + `motor` | 整机稳定行走      |

**验收**：整机可站立、卧倒、连续前进/后退。

---

### 阶段四：语音与 MCP 对接

| 任务                  | 负责模块                | 完成标准           |
| --------------------- | ----------------------- | ------------------ |
| MCP 调用 `DogControl` | `esp_sparkbot_board.cc` | 不再依赖 UART 底盘 |
| 语音链路              | 主项目 + 本板           | 唤醒后指令可达     |

**验收**：语音可可靠触发前进、后退、站立、卧倒（及已注册的 chassis 能力）。

---

### 阶段五：跳舞与触摸扩展

| 任务                       | 负责模块     | 完成标准                       |
| -------------------------- | ------------ | ------------------------------ |
| 跳舞序列                   | `dog`        | 可复现的一段舞蹈               |
| 一大步 / 持续走 / MCP 参数 | `dog` + 板级 | 与 README 描述一致             |
| 触摸映射                   | `touch_btn`  | init、组合键、小步/大步/持续走 |

**验收**：语音「跳舞」可触发；触摸逻辑与 `dog/README`、`touch_btn/README` 一致。

---

## 5. 运动域分册（六～九、十一～十三）

> **阶段十（视觉）已迁至** [../vision/](../vision/) 与 [../ROADMAP.md](../ROADMAP.md)，本文不再维护。


### 阶段六：最大力矩（堵转）保护

**需求**：电机侧记录堵转或异常负载时的力矩特征；先**标定**经验堵转力矩量级，再设**阈值**，超限时**失能**以保护减速器与绕组。

| 子步         | 内容                                                                             | 实现要点                                                                              |
| ------------ | -------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------- |
| 6.1 标定     | 安全环境下缓慢增加负载或卡死单轴，记录反馈帧中的力矩（协议已有 `INT2TORQUE` 等） | `motor/`：从反馈解析力矩、日志或临时 MCP 读出；**文档化**各关节典型堵转区间           |
| 6.2 策略     | 每轴滑动峰值/滤波后与安全阈值比较                                                | 阈值可配置（NVS / 常量）；触发后 `disable` 或进入安全态；可选仅告警不停机（调试开关） |
| 6.3 整机联动 | 单轴保护是否触发整机 `UnknownPose` / 停持续走                                    | 与 `DogStateMachine` 对齐，避免半瘫继续发步态                                         |

**测试与验证**

- 台架：单轴轻载/阻挡，确认日志力矩与 hand-check 一致。
- 保护：设定低于实测堵转的阈值，确认触发后电机失能且总线无异常风暴。
- 回归：正常行走不误触发（需滤波与持续时间条件，避免噪声误判）。

---

### 阶段七：大步轨迹插值 + 运控模式

**需求 A**：当前「一大步」= 半周期内 **N 个小步**（如 10 步）逐步下发；希望**插值加密**（例如 10→100 点），间隔同比缩小（×0.1），并扣除**报文与调度耗时**使实际轨迹更顺滑。

| 子步 | 内容                                                                  | 实现要点                                                                                                      |
| ---- | --------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------- |
| 7.1  | 在 `goForwardBigStep` / `goBackBigStep`（或下层）生成密集中间关节目标 | 复用 `trajectory/` 或对每关节做线性/样条插值；**总时长**与原大步可配置一致或与 `inter_step_delay_ms` 推导一致 |
| 7.2  | 计时                                                                  | 用 `esp_timer` / `vTaskDelay` 测量单周期 CAN 发送耗时，反推最大可行频率与插值点数上限                         |

**需求 B**：由「每点位置 + 速度限制」为主，改为**运控（MIT/小米式 p/v/kp/kd/tau）** 为主，提升刚性与抗扰（需与电机固件能力一致）。

| 子步 | 内容               | 实现要点                                                                                                                                                     |
| ---- | ------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 7.3  | 协议层封装运控发送 | ✅ `MotorProtocol::controlMotor`（p/v/kp/kd）+ `initializeMotorMitMode`；`DeepMotor::setMotorMitCommand`                                                     |
| 7.4  | 上层适配           | ✅ `LegControl`/`DogControl` 内 `#if DEEP_DOG_USE_MIT_WALK` + `DEEP_DOG_MIT_DEFAULT_*`；`config.h`：`DEEP_DOG_USE_MIT_WALK`、`DEEP_DOG_MIT_VALIDATE_FL_ONLY` |

**测试与验证**

- 对比示波/录包：同一大步，插值前后机身冲击与电流尖峰。
- 运控：阶跃与斜坡下超调、稳态误差；与现位置模式 A/B 对比；**台架**可仅 FL：`DEEP_DOG_MIT_VALIDATE_FL_ONLY`，初始化后站立，按键 2/3 单步前/后验证帧与动作。
- 压力：高插值频率下 CPU 占用与 CAN 负载率。

---

### 阶段八：身体左右晃、左/右移动作与跳舞扩展 — **已完成**

**需求**：增加**左右晃身**、**向左/向右走**（或横移语义），使跳舞与展示动作更丰富。

| 子步 | 内容      | 实现要点                                                                       |
| ---- | --------- | ------------------------------------------------------------------------------ |
| 8.1  | 晃身      | 髋侧摆/肩滚类关节按正弦或关键帧摆动，时长与幅值参数化                          |
| 8.2  | 横移/转向 | 在 `GaitPlanner` 或新步态向量上引入左右分量（可与未完成「左转/右转」合并设计） |
| 8.3  | 跳舞编排  | 将现有固定序列扩展为多段（含晃身、横移）；可选接 `trajectory` 做段间平滑       |

**测试与验证（已通过则打勾留存）**

- 地面标记：左右走偏航角与步长可重复性。
- MCP / 触摸 / 语音：与 `DogControl` 扩展接口一致；`self.chassis.sway` / `strafe_*` 等与主项目 MCP 配额统筹。

**状态**：本阶段作为**动作层**迭代已关闭；后续仅在发现 bug 或新增动作时小步修补。

---

### 阶段九：加速度传感器数据

**需求**：读取加速度计（及陀螺），供姿态估计、跌倒检测、闭环步态前置；并经 MQTT 上报。

| 子步 | 内容           | 实现要点                                      |
| ---- | -------------- | --------------------------------------------- |
| 9.1  | 硬件确认       | **型号拍板 BMI270**（与 [`deep-thumble/sensor`](../../../deep-thumble/sensor/) 一致）；接线写入 `config.h` I2C |
| 9.2  | 驱动与采样任务 | 可移植 thumble `ImuSensor`；固定频率读取；可选简单低通 |
| 9.3  | 对外           | 日志 / MCP 只读 / ROS 预留；**MQTT `imu/status`**（见 [M01](../mqtt/M01-board-mqtt-protocol.md) / [YAML](../mqtt/protocol/deep-dog-mqtt.yml)） |

**MQTT 字段（摘要）**：`ok`、`accel_x/y/z`（m/s²）、`gyro_x/y/z`（dps）、`accel_g`、`pitch`/`roll`（deg）、`ts`；建议发布约 5–10 Hz。

**测试与验证**

- 静态：Z 轴约 1g，水平放置三轴合理。
- 动态：晃动时波形连续；与阶段七冲击测试可交叉验证。
- MQTT：订阅 `.../imu/status` 可见与日志一致的加速度。

---

### 阶段十一：RK3588 CAN 与小智通讯

**需求**：RK3588 与当前小智（ESP32）通过 **CAN**（或 CAN-FD）交换高层指令、状态、传感器摘要，形成异构计算分工（规划/视觉在 3588，实时关节在 ESP32）。

| 子步 | 内容     | 实现要点                                                            |
| ---- | -------- | ------------------------------------------------------------------- |
| 11.1 | 帧定义   | 与电机协议 ID 空间隔离（不同 CAN ID 段或网关）                      |
| 11.2 | ESP32 侧 | TWAI 双通道或单总线网关策略；接收 3588 目标、回传里程/电量/IMU 摘要 |
| 11.3 | 3588 侧  | SocketCAN 用户态节点；与 ROS 可同进程或桥接                         |

**测试与验证**

- 环回与误码率；高负载下与电机帧共存是否丢包。
- 安全：非法指令过滤、看门狗。

---

### 阶段十二：ROS 状态/控制反馈与 rviz 显示

**需求**：将实时控制指令或关节/机身状态反馈到 **ROS**，在 **rviz** 中可视化（模型可用简化 URDF 或 marker）。

| 子步 | 内容     | 实现要点                                                                                                                 |
| ---- | -------- | ------------------------------------------------------------------------------------------------------------------------ |
| 12.1 | 桥接位置 | 优先在 RK3588 上跑 `ros2` 节点，通过 CAN/TCP 收 ESP32 状态；或 ESP32 侧 Wi‑Fi `ros_serial`/`micro_ros`（资源占用需评估） |
| 12.2 | 消息     | `sensor_msgs/JointState`、`geometry_msgs/Twist`（若适用）、自定义 `DogStatus`                                            |
| 12.3 | rviz     | Fixed frame、关节 display、可选 TF                                                                                       |

**测试与验证**

- 与真实狗动作延迟对比 rviz 显示；录 bag 复现。

---

### 阶段十三：基于 ROS 下发指令控制机器狗

**需求**：从 ROS **订阅**速度/步态/模式话题或 Action，转为 ESP32 可执行的 MCP 等价语义或 CAN 高层指令。

| 子步 | 内容     | 实现要点                                             |
| ---- | -------- | ---------------------------------------------------- |
| 13.1 | 指令映射 | 与现有 `DogControl` API 一一对应，禁止绕过安全状态机 |
| 13.2 | 优先级   | ROS vs 语音 vs 触摸冲突策略（互斥或优先级队列）      |
| 13.3 | 仿真     | 可选 Gazebo 仅在上位机，与真机共用同一话题名         |

**测试与验证**

- 急停与失能路径在 ROS 触发下仍有效。
- 与阶段六力矩保护、阶段七运控模式联合回归。

---

## 6. 依赖关系（更新）

```
can → motor → leg → dog → 板级 MCP / 触摸
         ↘ 力矩反馈 / 运控（阶段六、七）
dog / leg → trajectory（阶段七）
dog → 动作库（阶段八 ✅）
网页 /api/cmd → DogControl（视觉轨 S01，见 ROADMAP）
IMU（阶段九）→ ROS（阶段十二）
RK3588 CAN（十一）→ ROS（十二、十三）
```

---

## 7. 注意事项（保留）

- **目录边界**：新代码优先 `main/boards/deep-dog/`；动主工程保持最小化。
- **编译**：`CONFIG_BOARD_TYPE_DEEP_DOG` 时本目录递归参与编译。
- **头文件**：`#include "dog/dog_control.h"`、`#include "can/ESP32-TWAI-CAN.hpp"` 等相对路径。
- **子模块细节**：见各子目录 `README.md`（`can`、`motor`、`leg`、`trajectory`、`dog`、`touch_btn`）。
- **视觉 / 人脸 / 推流**：见 [../ROADMAP.md](../ROADMAP.md)；运动代码仍禁止在 `httpd` 回调内长时间调用 `DogControl`（由 `dog_web_cmd` 队列执行）。

---

## 8. 文档维护

| 日期       | 变更摘要                                                                                                                                           |
| ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-07-19 | 视觉整段迁出至 `vision/` + `ROADMAP.md`；本文仅保留运动域。 |
| （早前）   | 路线图与基线多次迭代。 |

各需求排期时可从「路线图总览」拆迭代；每阶段关闭时更新**基线表**与**文档维护**节。
