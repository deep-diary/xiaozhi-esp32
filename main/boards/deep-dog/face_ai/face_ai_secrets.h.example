/**
 * 联调 Immich API Key（编译期默认，写入 NVS 前仅在本地使用）。
 * 复制本文件为 face_ai_secrets.h 并取消下行注释；face_ai_secrets.h 已 gitignore。
 */
#pragma once

/* #define DEEP_DOG_FACE_IMMICH_SECRET_API_KEY "your-immich-api-key" */
